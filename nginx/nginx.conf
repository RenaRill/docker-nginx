events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location / {
            proxy_pass https://docker-nginx-client-xmk2.onrender.com;
        }

        # маршрутизация запросов на бэк
        location /api/ {
            proxy_pass https://docker-nginx-server-aa72.onrender.com;
        }
    }
}
